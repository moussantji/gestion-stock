// ============================================================
// StockFlow PC — configuration
// L'URL de l'API est modifiable dans ⚙️ Réglages (persistée).
// ============================================================
const SFPC_CONFIG = {
  DEFAULT_API_URL: 'http://127.0.0.1:8000/api', // même backend que l'app mobile
  APP_VERSION: 'StockFlow PC v2.14',
  BRAND: 'StockFlow',
};
