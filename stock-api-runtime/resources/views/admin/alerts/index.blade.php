@extends('layouts.admin')
@section('title','Alertes stock')
@section('content')
<div class="page-heading"><div><div class="eyebrow">SURVEILLANCE</div><h1 class="page-title">Alertes stock</h1><p class="page-sub">Produits à réapprovisionner ou actuellement en rupture.</p></div></div>
<div class="filters"><a class="btn {{ request('kind') === 'low' ? 'btn-primary' : 'btn-ghost' }}" href="{{ route('admin.alerts.index', ['kind'=>'low']) }}">Stock bas</a><a class="btn {{ request('kind') === 'out' ? 'btn-primary' : 'btn-ghost' }}" href="{{ route('admin.alerts.index', ['kind'=>'out']) }}">Ruptures</a><a class="btn {{ !request('kind') ? 'btn-primary' : 'btn-ghost' }}" href="{{ route('admin.alerts.index') }}">Toutes</a></div>
<form method="GET" class="filters"><input type="hidden" name="kind" value="{{ request('kind') }}"><select class="input" name="category_id"><option value="">Toutes les catégories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category_id') == $category->id)>{{ $category->name }}</option>@endforeach</select><select class="input" name="supplier_id"><option value="">Tous les fournisseurs</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}" @selected(request('supplier_id') == $supplier->id)>{{ $supplier->name }}</option>@endforeach</select><button class="btn btn-ghost">Filtrer</button></form>
<div class="card"><div class="card-title">{{ $products->total() }} alerte(s)</div><div class="table-wrap"><table><thead><tr><th>Produit</th><th>Catégorie</th><th>Fournisseur</th><th>Stock</th><th>Seuil</th><th>Action</th></tr></thead><tbody>@forelse($products as $product)<tr><td><strong>{{ $product->name }}</strong><div class="muted">{{ $product->sku }}</div></td><td>{{ $product->category?->name ?? '—' }}</td><td>{{ $product->supplier?->name ?? '—' }}</td><td class="{{ $product->quantity === 0 ? 'danger-text' : '' }}">{{ $product->quantity }}</td><td>{{ $product->alert_threshold }}</td><td><a class="btn btn-ghost btn-xs" href="{{ route('admin.movements.create', ['product_id'=>$product->id]) }}">Réapprovisionner</a></td></tr>@empty<tr><td colspan="6" class="table-empty">Aucune alerte active.</td></tr>@endforelse</tbody></table></div>{{ $products->links() }}</div>
@endsection
